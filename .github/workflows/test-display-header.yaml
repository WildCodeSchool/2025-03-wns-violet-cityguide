name: test-display-header

on:
  pull_request:
      types: [opened]

jobs:
  display-header:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Goto client and run tests
        run: cd client && npm i && npm run test